name: Finalize hard-coded JWT cleanup

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Finalization moved to the pull-request workflow"
