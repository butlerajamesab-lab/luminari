name: Provenance Alert Runtime Contract

on:
  pull_request:
    paths:
      - "server/provenance-alert-runtime-compat.ts"
      - "server/provenance-alert-runtime.contract.test.ts"
      - "server/provenance-alerting.ts"
      - "server/db.ts"
      - ".github/workflows/provenance-alert-runtime-contract.yml"
  push:
    branches: [main]
    paths:
      - "server/provenance-alert-runtime-compat.ts"
      - "server/provenance-alert-runtime.contract.test.ts"
      - "server/provenance-alerting.ts"
      - "server/db.ts"
      - ".github/workflows/provenance-alert-runtime-contract.yml"

jobs:
  provenance-alert-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec vitest run server/provenance-alert-runtime.contract.test.ts
      - run: pnpm run build
