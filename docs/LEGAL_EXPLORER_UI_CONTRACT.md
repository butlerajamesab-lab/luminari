# Legal Explorer UI Contract

The visible force/diagram surface is a bounded rendering window, never a data cap.

The page must show full-universe totals next to loaded-window counts, provide current node-type filters and text/jurisdiction search, and keep all governed reference catalogs reachable.

A user must never infer that the count of rendered circles equals the count of records in Luminari.
