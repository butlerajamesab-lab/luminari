# Merge Note

Merge this branch to make the existing live registry resource projection visible through the Lighthouse resources compatibility route.

The Supabase projection fix has already been applied live and verified.

Do not treat this PR as completion of the whole corpus pipeline. It is the visibility/cutover step for resources.
