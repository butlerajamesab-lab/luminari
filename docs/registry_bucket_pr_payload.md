# Registry Bucket PR Payload

Title:

```text
Expose live registry bucket resources through Lighthouse resource projection
```

Head:

```text
agent/lighthouse-registry-bucket-custody-index
```

Base:

```text
main
```
