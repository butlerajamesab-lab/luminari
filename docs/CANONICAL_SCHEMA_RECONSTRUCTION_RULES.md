# Canonical Schema Reconstruction Rules

Date: 2026-05-11

## Purpose

Define the canonical reconstruction rules for the upgraded Lighthouse/Luminari deterministic schema.

This document exists to prevent future schema drift and prevent reconstruction from legacy runtime contamination.

It is paired with `docs/DATABASE_CONTINUITY_GUARDRAILS.md`, which forbids using a fresh database as a shortcut around recovery drift; `docs/LUMINARI_FIVE_SUBSTRATE_PLATFORM.md`, which defines Atlas, Lighthouse, Prism, Rosetta, and Esquire ownership boundaries; `docs/ATLAS_PRISM_MATHEMATICAL_FOUNDATION.md`, which defines the Atlas-Prism signal, convergence, normalization, linking, and prioritization contract; `docs/ROSETTA_V3_LOCKED_MACHINE_CONTRACT.md`, which defines the Rosetta V3 law-to-constants output contract consumed by Lighthouse only as validated external output; `docs/ATLAS_CURRENT_STATE_AND_LIGHTHOUSE_BRIDGE.md`, which defines the verified Atlas production-view bridge boundary; and `docs/ESQUIRE_PROCEDURAL_CONTINUITY_ROLE.md`, which defines Esquire's downstream procedural-continuity boundary.

---

# Canonical Reconstruction Principle

The upgraded Lighthouse/Luminari architecture is canonical within the wider five-substrate Luminari platform.

The old fragmented runtime is not canonical.

The schema must be rebuilt around:

- upgraded deterministic architecture
- upgraded runtime spine
- operational-core domains
- deterministic integration contracts, including Atlas production bridge views, Atlas-Prism outputs, Rosetta V3 locked-machine outputs, and Esquire procedural-continuity outputs only where Lighthouse depends on validated external substrate outputs
- verified backbone assets

NOT around:

- legacy TiDB runtime
- fragmented V* drift
- placeholder engines
- speculative schema manifests

---

# Allowed Reconstruction Sources

Canonical schema reconstruction may ONLY derive from:

## Runtime-backed sources

- EXISTING_GOOD table set
- KEEP_AND_BUILD table set
- upgraded router references
- upgraded service references
- upgraded worker references

## Verified deterministic backbone assets

- legal registries
- resource registries
- Atlas bridge structures
- signal infrastructure
- governance structures
- machine metadata
- canonical UI architecture

## Deterministic architecture documents

- canonical layer stack
- operational core plan
- deterministic integration contracts, including Atlas production bridge views, Atlas-Prism outputs, Rosetta V3 locked-machine outputs, and Esquire procedural-continuity outputs only where Lighthouse depends on validated external substrate outputs
- canonical runtime recovery state

---

# Forbidden Reconstruction Sources

Do NOT reconstruct from:

- TiDB-era drift
- mysqlTable schemas
- hardcoded MySQL/TiDB runtime paths
- placeholder engines
- dead runtime experiments
- duplicate V* generations
- speculative manifest-only structures
- disconnected experimental schema branches

---

# Canonical Deterministic Layer Ownership

## L0 Intake

Owns:

- intake records
- uploads
- manifests
- source registration

## L1 Ingestion + Provenance

Owns:

- document provenance
- snapshots
- ingestion chains
- immutable source tracking

## L2 Extraction + Structuring

Owns:

- extracted entities
- extracted events
- extracted dates
- extracted locations
- structured fields

## L3 Registry + Knowledge Backbone

Owns:

- statutes
- agencies
- resources
- forms
- workflows
- remedies
- escalation routes
- doctrine registries

## L4 Validation + Normalization

Owns:

- canonical mappings
- validation rules
- normalization
- deterministic canonicalization

## L5 Evidence / Claim / Finding

Owns:

- claims
- findings
- evidence links
- missing records
- contradiction candidates

## L6 Pattern + Signal

Owns:

- patterns
- signal events
- convergence structures
- bridge structures
- topology structures
- fingerprints

## L7 Interpretation

Projection-only deterministic boundary.

Must remain:

- read-only
- deterministic
- non-mutating
- non-authoritative

No runtime bypasses allowed.

## L8 Procedural / Action

Owns:

- workflows
- deadlines
- escalations
- procedural sequencing

## L9 Export / Assembly

Owns:

- exports
- packets
- statements
- evidence bundles

## L10 Operations / Oversight

Owns:

- governance
- audit
- machine registry
- health metrics
- runtime oversight

## L11 UI Surfaces

Owns:

- visibility
- presentation
- navigation
- operational interaction

---

# Operational-Core Schema Priority

The operational-core schema must stabilize first.

Priority domains:

## Governance

- governance_log
- metadata_machines
- machine_outputs
- pipeline_runs

## Legal Backbone

- legal_statutes
- legal_case_law
- legal_definitions
- legal_enforcement_records
- legal_weak_joints

## Resource Backbone

- civil_gideon_directory
- national_resources
- agencies
- forms
- remedies

## Signal Infrastructure

- detected_signals
- signal_events
- streams
- prime_patterns
- civic_map_signals

## Atlas Bridges

- atlas_lighthouse_signal_bridge_v1
- atlas_lighthouse_resource_bridge_v1
- atlas_lighthouse_judicial_signal_bridge_v1
- atlas_lighthouse_legal_bridge_v1

---

# Deferred Reconstruction Domains

These remain deferred until canonical operational-core stabilization completes:

- full workflow orchestration
- escalation automation
- procedural mutation chains
- export mutation chains
- action queue automation
- full interpretation-driven action surfaces

---

# Canonical Naming Rules

Canonical naming must avoid drift.

Preferred forms:

- legal_statute
- signal_event
- signal_pattern
- bridge_atlas_signal
- governance_log
- workflow_step

Avoid:

- _v2
- _final
- _fixed
- _real
- _new
- _rebuild
- duplicate V* runtime generations

---

# Canonical Runtime Rule

The architecture is canonical.

The implementation must conform to the architecture.

Implementation drift is not authoritative.
