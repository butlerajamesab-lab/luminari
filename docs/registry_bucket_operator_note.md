# Operator Note

This branch makes Lighthouse's resource surface read the live resource projection.

It does **not** pretend the entire registry corpus is finished.

The live registry corpus now needs two separate follow-up tracks:

1. **Visibility track:** merge/deploy this branch so the existing 11,871-resource projection is visible.
2. **Completion track:** repair storage materialization, source hashes, manifest state, SAIS resources, and deadline rules.
