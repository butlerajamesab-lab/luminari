# Lighthouse Evidence/Graph Expansion Migration Library

This directory preserves future Lighthouse evidence/graph/workflow expansion migration material as **schema-library content only**.

The current gate is `lighthouse_runtime_backend_lock`. Files in this directory are **not authorized for execution during the current gate**.

| Field | Value |
|---|---|
| Migration | Lighthouse Evidence/Graph Expansion Migration |
| Migration ID | `lighthouse_evidence_graph_expansion_001` |
| Target project ref | `wepxlinwbjrkqdzkqpar` |
| Target database | Supabase PostgreSQL |
| Schema layer | `evidence_graph_expansion` |
| Status | `library_only_not_executed` |
| Run now | `false` |
| Current gate authorized | `false` |
| Current gate | `lighthouse_runtime_backend_lock` |

Do not confuse schema-library inclusion with execution approval. This package is retained so the architecture is not lost, but it must not be run unless a future graph-layer activation gate explicitly authorizes it.
