// The historical category/jurisdiction prototype remains in git history, but
// the production Guided Intake routes now use the existing deterministic,
// case-centered Lighthouse intake surface.
export { default } from "./GuidedIntake";
