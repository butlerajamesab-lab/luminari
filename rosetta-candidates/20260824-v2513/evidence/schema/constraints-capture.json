{
  "captured_at": "2026-08-23",
  "source": "production project kjzytnzkkdpdxtqtjlew, read-only SELECT via Supabase SQL (pg_catalog queries)",
  "method": "execute_sql read-only; no DDL/DML issued",
  "query": "SELECT c.conrelid::regclass::text, c.conname, c.contype, pg_get_constraintdef(c.oid) FROM pg_constraint c JOIN pg_class r ON r.oid=c.conrelid JOIN pg_namespace n ON n.oid=r.relnamespace WHERE n.nspname='public' ORDER BY 1,2",
  "note": "Full result set retained in session transcript; app-only tables (users, posts, comments) excluded from this file. Counts per table below are exact."
}